`git clone https://github.com/eagle-shop/gl_test`  
`cd gl_test`  
`git submodule update --init --recursive`  
`mkdir build`  
`cd build`  

`cmake ..`  
`cmake --build .`  
or (for macOS)  
`cmake .. -G Xcode`
